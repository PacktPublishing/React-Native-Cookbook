/**
 * Saving and reading local data with AsyncStorage
 * @flow
 */

import React from 'react';
import { AppRegistry } from 'react-native';
import MainApp from './src/MainApp';

AppRegistry.registerComponent('SavingLocally', () => MainApp);
